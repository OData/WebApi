name: .NET Core

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]

    steps:
    - uses: actions/checkout@v1
    - name: Setup .NET Core 2.1.202
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 2.1.202
    - name: Setup .NET Core 3.1.301
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.301
    - name: Setup side by side .NET SDKs on Windows
      if: matrix.os == 'windows-latest'
      run: |
        SET DOTNET_22_ROOT=%DOTNET_ROOT:3.1.301=2.1.202%
        xcopy /s /y /d %DOTNET_31_ROOT% %DOTNET_ROOT%
      shell: cmd
    - name: Setup side by side .NET SDKs on ${{ matrix.os }}
      if: matrix.os != 'windows-latest'
      run: |
        rsync -a ${DOTNET_ROOT/3.1.301/2.1.202}/* $DOTNET_ROOT/
    - name: Build with netcoreapp
      run: dotnet build '/sln/WebApiOData.AspNetCore.sln' --configuration Release
    - name: Unit Testing
      run: dotnet test
